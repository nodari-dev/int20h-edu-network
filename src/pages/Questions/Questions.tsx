import { FC } from "react";
import { Flex } from "antd";

interface IProps {}

export const Questions: FC<IProps> = (): JSX.Element => {

  return (
    <Flex gap="small" vertical>
      Questions
    </Flex>
  );
};